export type TAboutHero = {
  title: string;
  description: string;
  service_one: string;
  service_two: string;
  service_three: string;
  service_four: string;
  service_five?: string;
  image: string;
};
