export type TProfessionalService = {
  title: string;
  description: string;
  service_one: string;
  service_tow: string;
  service_three: string;
  service_four: string;
  color_code: string;
  image: string;
};
